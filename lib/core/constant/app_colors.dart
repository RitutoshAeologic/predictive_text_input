import 'package:flutter/material.dart';

class AppColors {
  // Background layers
  static const bg         = Color(0xFFFAF8F5);       // warm off-white
  static const surface    = Color(0xFFF2EFE9);       // slightly warmer
  static const card       = Color(0xFFFFFFFF);       // pure white for cards
  static const keyBar     = Color(0xFFEDEAE3);       // keyboard bar bg

  // Accent — warm amber/gold
  static const accent     = Color(0xFFD4882A);
  static const accentSoft = Color(0xFFFFF3DC);       // accent tint
  static const accentDeep = Color(0xFFAA6A18);

  // Text
  static const textPrimary   = Color(0xFF1C1917);
  static const textSecondary = Color(0xFF78716C);
  static const textHint      = Color(0xFFA8A29E);
  static const textOnAccent  = Color(0xFFFFFFFF);

  // Borders / dividers
  static const border     = Color(0xFFE7E3DC);
  static const divider    = Color(0xFFD6D1C8);

  // Chip states
  static const chipBg     = Color(0xFFFFFFFF);
  static const chipBorder = Color(0xFFDDD8D0);
}
